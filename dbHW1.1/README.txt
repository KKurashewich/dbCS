Compile: to compile type "make" in directory with the makefile

start: To Run Excutable type "./main"

notes on locations:
    "/lib" directory contains .cpp and .h(header) files
    "/obj" directory contains .o(object files)
    "/dataBaseCollection" directory contains the created dataBases